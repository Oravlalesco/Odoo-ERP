apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: odoo
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: erp-wms-tms
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
